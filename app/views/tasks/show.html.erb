<h1>id = <%= @tasks.id %> の詳細ページ</h1>

<table class="table table-bordered">
  <tr>
    <th>id</th>
    <td><%= @tasks.id %></td>
  </tr>
  <tr>
    <th>タスク</th>
    <td><%= @tasks.content %></td>
  </tr>
  <tr>
    <th>ステータス</th>
    <td><%= @tasks.title %></td>
  </tr>
</table>

<%= link_to 'この投稿を編集する', edit_task_path(@tasks), class: 'btn btn-default' %>
<%= link_to 'この投稿を削除する', @tasks, method: :delete, data: { confirm: '本当に削除してよろしいですか？' }, class: 'btn btn-danger' %>